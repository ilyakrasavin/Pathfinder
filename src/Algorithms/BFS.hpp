#include "../Graph.hpp"


int BreadthFirstSearch(Node* Start, sf::RenderWindow* windowRef){

// Run the algorithm AND perform rendering within the algoritm

// ASSURE no retraction is happenning (No backwards exporation -> Nodes marked visited)

}